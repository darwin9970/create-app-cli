# create-app-cli

🚀 一个健壮、易用的项目脚手架工具，用于快速创建 Vue/React 项目。

## ✨ 特性

- 📦 **多种模板** - 支持 5 种主流技术栈模板
- 🛡️ **健壮可靠** - 完善的错误处理和验证机制
- 🎨 **交互友好** - 美观的命令行交互界面
- ⚡ **开箱即用** - 自动安装依赖和初始化 Git
- 🔧 **灵活配置** - 支持跳过安装或 Git 初始化

## 📋 可用模板

| 模板名称            | 技术栈                                         | 描述                |
| ------------------- | ---------------------------------------------- | ------------------- |
| `vit6+vue3`         | Vite 6 + Vue 3 + Vant                          | 移动端项目模板      |
| `vite6+react19`     | Vite 6 + React 19 + Ant Design                 | React Web 应用      |
| `vite6+react19+ts5` | Vite 6 + React 19 + TypeScript 5 + Ant Design  | React + TS Web 应用 |
| `vite6+vue3+ts5`    | Vite 6 + Vue 3 + TypeScript 5 + Ant Design Vue | Vue + TS Web 应用   |
| `wxt+vue3`          | WXT + Vue 3                                    | 浏览器扩展模板      |

## � 前提条件

- Node.js >= 16.0.0
- npm / yarn / pnpm
- Git（可选，用于版本控制）

## � 快速开始

### 本地安装

```bash
# 1. 进入脚手架目录
cd create-app-cli/

# 2. 安装依赖
npm install

# 3. 链接到全局（使 create-app 命令可用）
npm link

# 4. 验证安装
create-app --version
```

### 使用

#### 1. 创建项目

```bash
# 交互式创建（推荐）
create-app

# 指定项目名称
create-app my-project

# 跳过依赖安装
create-app my-project --no-install

# 跳过 Git 初始化
create-app my-project --no-git
```

#### 2. 启动项目

```bash
# 进入项目目录
cd my-project

# 启动开发服务器
npm run dev

# 浏览器会自动打开 http://localhost:5173
```

#### 3. 其他命令

```bash
# 查看可用模板
create-app list

# 查看帮助
create-app --help

# 查看版本
create-app --version
```

## 📖 使用流程

1. **运行命令**

   ```bash
   create-app my-app
   ```

2. **选择模板**

   - 系统会列出所有可用模板
   - 使用方向键选择，回车确认

3. **配置选项**

   - 是否自动安装依赖？
   - 是否初始化 Git 仓库？

4. **等待创建**

   - 复制模板文件
   - 更新项目配置
   - 安装依赖（可选）
   - 初始化 Git（可选）

5. **开始开发**
   ```bash
   cd my-app
   npm run dev
   ```

## 💡 使用示例

### 创建 Vue3 移动端项目

```bash
create-app mobile-app
# 选择: vit6+vue3
cd mobile-app
npm run dev
```

### 创建 React + TypeScript 项目

```bash
create-app react-ts-app
# 选择: vite6+react19+ts5
cd react-ts-app
npm run dev
```

### 创建浏览器扩展

```bash
create-app my-extension
# 选择: wxt+vue3
cd my-extension
npm run dev
```

### CI/CD 环境使用

```bash
# 只复制模板，不安装依赖
create-app my-app --no-install --no-git
cd my-app
pnpm install  # 使用特定的包管理器
```

## 🔧 开发和测试

如果你想修改或扩展脚手架工具：

```bash
# 进入脚手架目录
cd /Users/xiangxiwen/Documents/template/create-app-cli

# 运行测试（验证所有功能）
./test.sh

# 运行演示（查看功能展示）
./demo.sh

# 修改代码后，测试命令
create-app test-project

# 添加新模板
# 1. 在 ../template/ 目录下创建新模板
# 2. 编辑 lib/utils.js 的 getAvailableTemplates() 函数
```

## 🛡️ 错误处理

脚手架包含完善的错误处理机制：

- ✅ **项目名称验证** - 确保符合 npm 包命名规范
- ✅ **目录检查** - 自动检测目录是否存在
- ✅ **模板验证** - 验证模板完整性
- ✅ **依赖安装失败处理** - 提供手动安装提示
- ✅ **Git 初始化失败处理** - 优雅降级
- ✅ **文件复制失败自动清理** - 避免留下不完整的项目

## 📝 命令行选项

| 选项            | 描述                |
| --------------- | ------------------- |
| `--no-install`  | 跳过依赖安装        |
| `--no-git`      | 跳过 Git 仓库初始化 |
| `-v, --version` | 显示版本号          |
| `-h, --help`    | 显示帮助信息        |

## 🎯 最佳实践

### 1. 项目名称规范

- 使用小写字母
- 使用连字符分隔单词（如 `my-project`）
- 避免特殊字符和空格

### 2. 模板选择指南

- **移动端应用** → 选择 `vit6+vue3`（包含 Vant UI）
- **React 项目** → 选择 `vite6+react19` 或 `vite6+react19+ts5`
- **Vue 项目** → 选择 `vite6+vue3+ts5`
- **浏览器扩展** → 选择 `wxt+vue3`
- **需要 TypeScript** → 选择带 `ts5` 的模板

### 3. 开发流程建议

```bash
# 1. 创建项目
create-app my-project

# 2. 进入项目
cd my-project

# 3. 启动开发服务器
npm run dev

# 4. 开发模式（支持热更新）
# 修改代码会自动刷新浏览器

# 5. 生产构建
npm run build:pro

# 6. 预览生产构建
npm run preview
```

### 4. 依赖管理

- 推荐使用 **pnpm**（速度快，节省空间）
- CI/CD 环境建议使用 `--no-install` 然后手动安装

## ⚠️ 注意事项

- **Node.js 版本**: 需要 >= 16.0.0（推荐使用 LTS 版本）
- **磁盘空间**: 确保有足够空间（每个项目约 200-500MB）
- **网络环境**: 需要能访问 npm registry（建议配置国内镜像）
- **权限问题**: macOS/Linux 可能需要 `sudo npm link`
- **端口占用**: 默认使用 5173 端口，确保未被占用
- **首次运行**: 第一次安装依赖会比较慢，请耐心等待

## ❓ 常见问题

### Q: 如何卸载？

```bash
npm unlink create-app-cli
# 或
npm uninstall -g create-app-cli
```

### Q: 创建失败了怎么办？

1. 检查 Node.js 版本（需要 >= 16.0.0）
2. 确保有网络连接
3. 检查磁盘空间
4. 查看错误信息并按提示操作

### Q: 可以自定义模板吗？

可以！在 `/Users/xiangxiwen/Documents/template/` 目录下添加新的模板目录，然后在 `lib/utils.js` 的 `getAvailableTemplates()` 函数中添加配置。

### Q: 依赖安装很慢？

建议使用 pnpm 或配置镜像：

```bash
# 使用 pnpm
npm install -g pnpm

# 或配置 npm 镜像
npm config set registry https://registry.npmmirror.com
```

### Q: 命令找不到？

```bash
# 重新链接
npm link

# 或使用 sudo（macOS/Linux）
sudo npm link
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 License

MIT

## 👨‍💻 作者

xiangxiwen

---

**Happy Coding! 🎉**
